'use strict';
const test=require('node:test'),assert=require('node:assert/strict');const{upsertManagedBody,inspectManagedBody}=require('../src/managed-sections');
test('managed sections preserve human prose and update idempotently',()=>{const initial='Human context';const once=upsertManagedBody(initial,{summary:'## Summary\nA'}),twice=upsertManagedBody(once,{summary:'## Summary\nB'});assert.ok(twice.startsWith('Human context'));assert.equal((twice.match(/codex-change-safe:summary:start/g)||[]).length,1);assert.ok(twice.includes('## Summary\nB'));});
test('duplicate or malformed markers fail closed',()=>{const bad='<!-- codex-change-safe:summary:start -->\nA\n<!-- codex-change-safe:summary:start -->';assert.equal(inspectManagedBody(bad).valid,false);assert.throws(()=>upsertManagedBody(bad,{summary:'B'}),{code:'EMANAGEDMARKERS'});});
