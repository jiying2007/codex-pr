'use strict';
const test=require('node:test'),assert=require('node:assert/strict');const{parseCodeowners,ownersForPath,suggestReviewers}=require('../src/codeowners');
test('GitHub CODEOWNERS keeps last-match semantics',()=>{const rules=parseCodeowners('*.js @alice\nsrc/** @bob\nsrc/audio/** @carol @alice @org/audio-team',{provider:'github'});assert.deepEqual(ownersForPath('src/audio/a.js',rules),{users:['carol','alice'],teams:['org/audio-team']});});
test('GitLab CODEOWNERS applies sections independently and inherits section defaults',()=>{const rules=parseCodeowners('[Backend] @backend\nsrc/**\n[Security] @sec\nsrc/auth/** @security-team',{provider:'gitlab'});assert.deepEqual(ownersForPath('src/auth/a.js',rules),{users:['backend','security-team'],teams:[]});});
test('GitLab exclusions clear only their section and roles are surfaced unresolved',()=>{const rules=parseCodeowners('[A] @alice\nsrc/**\n!src/generated/**\n[B] @@maintainer\nsrc/**',{provider:'gitlab'}),owners=ownersForPath('src/generated/x.js',rules),suggested=suggestReviewers(['src/generated/x.js'],rules);assert.deepEqual(owners,{users:[],teams:[]});assert.ok(suggested.unsupported.some(x=>x.reason==='gitlab_role_owner_requires_runtime_member_resolution'));});
