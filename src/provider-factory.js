'use strict';
const { detectProvider }=require('./remote'); const { GitHubProvider }=require('./providers/github'); const { GitLabProvider }=require('./providers/gitlab'); const GITHUB_CLOUD_API='https://api.github.com';
function assertApiBoundToRemote(kind,remote,apiBaseUrl){const api=new URL(apiBaseUrl),apiHost=api.hostname.toLowerCase(),remoteHost=remote.host.toLowerCase();const allowed=kind==='github'&&remoteHost==='github.com'?apiHost==='api.github.com':apiHost===remoteHost;if(!allowed)throw Object.assign(new Error(`Refusing to send ${kind} credentials to ${apiHost}; git remote host is ${remoteHost}.`),{code:'EAPIHOSTMISMATCH'});return apiBaseUrl;}
function githubApiBase(remote,configuredBaseUrl=''){const c=String(configuredBaseUrl||'').trim().replace(/\/$/,'');if(remote.host==='github.com')return c||GITHUB_CLOUD_API;if(!c||c===GITHUB_CLOUD_API)return`https://${remote.host}/api/v3`;return c;}
function createProvider(remote,config,sourceRemote=remote){const kind=detectProvider(remote,config);if(sourceRemote.host!==remote.host)throw Object.assign(new Error('Cross-host source/target delivery is not supported; source and target must be on the same SCM instance.'),{code:'ETOPOLOGYHOST'});const timeoutMs=Math.max(3000,Number(config.requestTimeoutSeconds||30)*1000),allowInsecureHttp=Boolean(config.allowInsecureHttp);if(kind==='github'){const base=assertApiBoundToRemote(kind,remote,githubApiBase(remote,config.githubApiBaseUrl));return new GitHubProvider({remote,sourceRemote,apiBaseUrl:base,token:process.env[config.githubTokenEnv||'GITHUB_TOKEN']||'',timeoutMs,allowInsecureHttp});}const base=assertApiBoundToRemote(kind,remote,config.gitlabApiBaseUrl||`https://${remote.host}/api/v4`);return new GitLabProvider({remote,sourceRemote,apiBaseUrl:base,token:process.env[config.gitlabTokenEnv||'GITLAB_TOKEN']||'',timeoutMs,allowInsecureHttp});}
module.exports={createProvider,assertApiBoundToRemote,githubApiBase,GITHUB_CLOUD_API};
